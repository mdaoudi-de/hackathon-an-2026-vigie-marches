# Dépendances d'exécution (moteur + ingestion + API + serveur MCP).
# Outillage de dev et de génération des livrables : voir requirements-dev.txt
requests>=2.31
duckdb>=1.0
tqdm>=4.66
pydantic>=2.7
rapidfuzz>=3.9
fastapi>=0.111
uvicorn[standard]>=0.30
anthropic>=0.40
mcp[cli]>=1.2
